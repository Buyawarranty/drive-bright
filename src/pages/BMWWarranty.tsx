// Render the same fully-featured BMW landing layout that matches the
// approved design (matches the user-supplied reference screenshot).
// Keeps URL /car-extended-warranty/bmw/ pointing at the canonical layout.
export { default } from './landing/BMWExtendedWarrantyLanding';
